# data files (large snapshots — generate with lucky.py > Update dataset)
all_numbers.csv
numbers_*.csv
numbers_prices.csv
watch_snapshot.json

# your personal numbers (keep private)
my_numbers.csv
watchlist.txt

# scratch
*.csv.tmp
found_*.csv
__pycache__/

# Vercel local project links + pull-generated env (project-specific, must not
# be committed — a stale committed link could target the wrong project)
webapp/.vercel/
vercel-fn/.vercel/

# buy bridge / worker runtime state (local only)
buy_queue.txt
buy_processed.log
buy_worker.log
buy_worker.lock
