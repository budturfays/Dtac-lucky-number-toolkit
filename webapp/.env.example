# URL of the Vercel serverless buy function (headless Chromium auto-select).
# If unset, the app falls back to: local bridge -> default Vercel URL -> open listing tab.
VITE_BUY_API=https://lucky-number-buy.vercel.app/api/buy
