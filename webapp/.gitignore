# deps & build
node_modules/
dist/

# Vercel local cache & secrets
.vercel/
.env.local

# Firebase local cache & secrets (Firebase is being removed)
.firebase/
scripts/service-account.json

# large generated data (rebuilt by export script / GitHub Actions)
public/data/numbers.json
public/data/meta.json

# logs & scratch
*.csv
watch_log.csv
_tmp_*.py
